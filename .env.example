# PostgreSQL - must match in both sections
POSTGRES_DB=odoo
POSTGRES_USER=odoo
POSTGRES_PASSWORD=changeme_strong_password

# Odoo DB connection (points to the db container)
HOST=db
USER=odoo
PASSWORD=changeme_strong_password
